services:
  qdrant:
    image: qdrant/qdrant:v1.12.4
    container_name: engineering-rag-qdrant
    ports:
      - "6333:6333"
    volumes:
      - qdrant_data:/qdrant/storage
    restart: unless-stopped

volumes:
  qdrant_data:
